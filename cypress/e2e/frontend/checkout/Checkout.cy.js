describe('', () => {
    beforeEach(() => {
        cy.visit('http://localhost:3000/cart');
        cy.wait(2000);
    })
    it('As a user, I want to checkout with existing items in my cart', () => {
        
    })
})